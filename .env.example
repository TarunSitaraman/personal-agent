WHATSAPP_TOKEN=
WHATSAPP_PHONE_ID=
WHATSAPP_VERIFY_TOKEN=jarvis_verify_2024
MY_WHATSAPP_NUMBER=91XXXXXXXXXX
# Numbers other than the owner permitted to register on first contact, comma-separated, same
# E.164-without-plus format as above. Leave empty to keep the agent closed to everyone else —
# the WhatsApp number is public, so an unset allowlist must mean "nobody", not "anybody".
ALLOWED_NUMBERS=
OPENROUTER_API_KEY=
GROQ_API_KEY=
SERPER_API_KEY=
GITHUB_TOKEN=
DASHBOARD_TOKEN=
DATABASE_URL=
PORT=3000
TZ=Asia/Kolkata
APP_URL=                 # Set to your Render URL if RENDER_EXTERNAL_URL isn't auto-set (e.g. https://your-app.onrender.com)
